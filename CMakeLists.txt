cmake_minimum_required(VERSION 2.8)
project(logger)

ENABLE_TESTING()
if(Apple)
  add_definitions(-Wall -Werror -Wno-unused-variable -Wno-unused-private-field)
else(Apple)
  add_definitions(-Wall -Werror -Wno-unused-variable)
endif(Apple)
add_subdirectory(gtest)
add_subdirectory(src)
